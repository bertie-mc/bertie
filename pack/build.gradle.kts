plugins {
    id("bertie.pack")
    id("bertie.gametest")
    id("bertie.client-test")
}

configurations.named("gametestComponents") {
    extendsFrom(configurations.named("packComponents").get())
}

configurations.named("clienttestComponents") {
    extendsFrom(configurations.named("packComponents").get())
}

dependencies {
    gametestImplementation(project(":mods:berlords-carving"))
    clienttestCompileOnly(deps.curios)
    clienttestCompileOnly(deps.terraCurio)

    packComponents(deps.accessdenied)
    packComponents(deps.accessoriesCompatLayer)
    packComponents(deps.advancedLootInfo)
    packComponents(deps.advancementPlaques)
    packComponents(deps.airJumpEnchantment)
    packComponents(deps.alexsCavesUnofficialPort)
    packComponents(deps.alexsMobs1211)
    packComponents(deps.alexsMobsExtraMusic)
    packComponents(deps.amendments)
    packComponents(deps.ancientForgemastery)
    packComponents(deps.antarchy)
    packComponents(deps.antiKnockbackEnchantment)
    packComponents(deps.anvilcraft)
    packComponents(deps.apotheosis)
    packComponents(deps.apotheosisXIronsSpellbooksCompat)
    packComponents(deps.apothicCombat)
    packComponents(deps.appleskin)
    packComponents(deps.aquaculture)
    packComponents(deps.aquacultureDelight)
    packComponents(deps.aquaticCreepers)
    packComponents(deps.armageddon)
    packComponents(deps.armorStatues)
    packComponents(deps.artifacts)
    packComponents(deps.attackSpeedEnchantment)
    packComponents(deps.avaritiasDelight)
    packComponents(deps.badWitherNoCookie)
    packComponents(deps.barteringStation)
    packComponents(deps.berriesAndCherries)
    packComponents(deps.betterAdvancements)
    packComponents(deps.betterArcheology)
    packComponents(deps.betterBeaconPlacement)
    packComponents(deps.betterClimbing)
    packComponents(deps.betterendNeoforge)
    packComponents(deps.betterf3)
    packComponents(deps.bettersmithingtable)
    packComponents(deps.bigBirchTree)
    packComponents(deps.bigOakTree)
    packComponents(deps.biomeReplacer)
    packComponents(deps.blockRunner)
    packComponents(deps.boatview360)
    packComponents(deps.borninchaos)
    packComponents(deps.bornInConfiguration)
    packComponents(deps.bossesrise)
    packComponents(deps.bowInfinityFix)
    packComponents(deps.brewinAndChewin)
    packComponents(deps.burntBasic)
    packComponents(deps.cableTiers)
    packComponents(deps.calcmodNeoforged)
    packComponents(deps.cataclysmSpellbooks)
    packComponents(deps.cataclysmTools)
    packComponents(deps.cataclysmXBettercombatCompat)
    packComponents(deps.certainQuestingAdditions)
    packComponents(deps.chalkMod)
    packComponents(deps.chipped)
    packComponents(deps.chunkLoaders)
    packComponents(deps.cinematicCataclysm)
    packComponents(deps.clientTweaks)
    packComponents(deps.clumps)
    packComponents(deps.coalExplosion)
    packComponents(deps.cocktailsDelight)
    packComponents(deps.cognition)
    packComponents(deps.combatEffects)
    packComponents(deps.comforts)
    packComponents(deps.commandkeys)
    packComponents(deps.compactMachines)
    packComponents(deps.compactmachinespor)
    packComponents(deps.complementaryReimagined)
    packComponents(deps.controlling)
    packComponents(deps.cosmeticArmorReworkedForked)
    packComponents(deps.crabbersDelight)
    packComponents(deps.crafttweaker)
    packComponents(deps.crateDelight)
    packComponents(deps.create)
    packComponents(deps.creeperOverhaul)
    packComponents(deps.cuisineDelight)
    packComponents(deps.culturalDelights)
    packComponents(deps.curseOfPandora)
    packComponents(deps.cutThrough)
    packComponents(deps.darkDoppelganger)
    packComponents(deps.deeperAndDarkerSpellbooks)
    packComponents(deps.deeperdarker)
    packComponents(deps.deepWaters)
    packComponents(deps.despawnTweaks)
    packComponents(deps.diagonalFences)
    packComponents(deps.diagonalWalls)
    packComponents(deps.diamondInTheRough)
    packComponents(deps.discerningTheEldritch)
    packComponents(deps.disEnchantingTable)
    packComponents(deps.distractionFreeRecipes)
    packComponents(deps.doesItTick)
    packComponents(deps.dracolotl)
    packComponents(deps.dungeonsAndTavernsWoodlandMansionOverhaul)
    packComponents(deps.dungeonsDelight)
    packComponents(deps.durabilityTooltip)
    packComponents(deps.dynamicOreVeins)
    packComponents(deps.easyAnvils)
    packComponents(deps.easyMagic)
    packComponents(deps.easyMagicApotheosisCompat)
    packComponents(deps.eccentricTome)
    packComponents(deps.echoChest)
    packComponents(deps.echoesOfTheEndStructures)
    packComponents(deps.elementalMetalsMagicalMetallurgySeries)
    packComponents(deps.elevatormod)
    packComponents(deps.elytraSlot)
    packComponents(deps.emendatusenigmatica)
    packComponents(deps.emi)
    packComponents(deps.emiEnchanting)
    packComponents(deps.emiffect)
    packComponents(deps.emiOres)
    packComponents(deps.emiProfessionsEmip)
    packComponents(deps.enderio)
    packComponents(deps.enderioEndergy)
    packComponents(deps.endermanOverhaul)
    packComponents(deps.endersDelight)
    packComponents(deps.endrem)
    packComponents(deps.endRemasteredAdditions)
    packComponents(deps.endsDelight)
    packComponents(deps.energymeter)
    packComponents(deps.enhancedBossBarsMod)
    packComponents(deps.enigmaticlegacy)
    packComponents(deps.entangled)
    packComponents(deps.epicStructuresWitchHuts)
    packComponents(deps.etched)
    packComponents(deps.euphoriaPatches)
    packComponents(deps.expandedDelight)
    packComponents(deps.explorersCompass)
    packComponents(deps.exposure)
    packComponents(deps.exposureExpanded)
    packComponents(deps.exposurePolaroid)
    packComponents(deps.extendedBoneMeal)
    packComponents(deps.extradelight)
    packComponents(deps.extraDisks)
    packComponents(deps.extraModIntegrations)
    packComponents(deps.extremeSoundMuffler)
    packComponents(deps.fantasyArmor)
    packComponents(deps.farmersKnives)
    packComponents(deps.farmersPizzeria)
    packComponents(deps.ferriteCore)
    packComponents(deps.feurExtensionDesert)
    packComponents(deps.fishingoverhaul)
    packComponents(deps.flightRing)
    packComponents(deps.forbiddenArcanus)
    packComponents(deps.forgifiedFabricApi)
    packComponents(deps.frightsDelight)
    packComponents(deps.ftbFilterSystem)
    packComponents(deps.ftbQuestsForge)
    packComponents(deps.ftbXmodCompat)
    packComponents(deps.gensokyoDelightYoukaisFeasts)
    packComponents(deps.giantCherryBlossomBonsaiTree)
    packComponents(deps.giantDeadOakTree)
    packComponents(deps.giantSnowyTree)
    packComponents(deps.giantTreeStump)
    packComponents(deps.glassbreaker)
    packComponents(deps.gravelminer)
    packComponents(deps.handcrafted)
    packComponents(deps.handOverYourItems)
    packComponents(deps.hangGlider)
    packComponents(deps.hazenNStuff)
    packComponents(deps.hellishTrials)
    packComponents(deps.hideExperimentalWarning)
    packComponents(deps.highlight)
    packComponents(deps.hooked)
    packComponents(deps.hopoBetterRuinedPortals)
    packComponents(deps.hopoBetterUnderwaterRuins)
    packComponents(deps.hostileNeuralNetworks)
    packComponents(deps.iceandfireCe)
    packComponents(deps.iceAndFireSpellbooks)
    packComponents(deps.illagerInvasion)
    packComponents(deps.immersiveArmors)
    packComponents(deps.immersiveEnchanting)
    packComponents(deps.immersiveGateways)
    packComponents(deps.improvedPillagerOutpost)
    packComponents(deps.incendium)
    packComponents(deps.inControl)
    packComponents(deps.interdimensionalWirelessTransmitter)
    packComponents(deps.inventoryManagementDeluxe)
    packComponents(deps.invmovecompats)
    packComponents(deps.iris)
    packComponents(deps.ironBookshelves)
    packComponents(deps.ironsSpellbooksArcaneEssenceBlocks)
    packComponents(deps.ironsSpellsNSpellbooks)
    packComponents(deps.ironsSpellsNSpellbooksRestricitons)
    packComponents(deps.itemBorders)
    packComponents(deps.itemCollectors)
    packComponents(deps.itemDescriptions)
    packComponents(deps.itemHighlighter)
    packComponents(deps.itemsNbtViewer)
    packComponents(deps.jadeAddonsForge)
    packComponents(deps.kaleidoscopeDoll)
    packComponents(deps.l2Complements)
    packComponents(deps.l2hostility)
    packComponents(deps.laserBridgesAndDoors)
    packComponents(deps.lavawalker)
    packComponents(deps.leavesBeGone)
    packComponents(deps.lEndersCataclysm)
    packComponents(deps.lEndersCataclysmDelight)
    packComponents(deps.lmd)
    packComponents(deps.lootr)
    packComponents(deps.lukisAncientCities)
    packComponents(deps.lukisCrazyChambers)
    packComponents(deps.lychee)
    packComponents(deps.macawsDoors)
    packComponents(deps.macawsTrapdoors)
    packComponents(deps.magicCoins)
    packComponents(deps.magicOfColor)
    packComponents(deps.magitech)
    packComponents(deps.magnumTorch)
    packComponents(deps.malstone)
    packComponents(deps.malum)
    packComponents(deps.malumVestis)
    packComponents(deps.mekanismAdditions)
    packComponents(deps.mekanismCovers)
    packComponents(deps.mekanismCurios)
    packComponents(deps.mekanismExtra)
    packComponents(deps.mekanismGenerators)
    packComponents(deps.mekanismmoremachine)
    packComponents(deps.mekanismPonders)
    packComponents(deps.mekanismTools)
    packComponents(deps.mesMoogsEndStructures)
    packComponents(deps.midnightThoughts)
    packComponents(deps.mindfulDarkness)
    packComponents(deps.minersDelight)
    packComponents(deps.mmmmmmmmmmmm)
    packComponents(deps.mobLassos)
    packComponents(deps.modelfix)
    packComponents(deps.modernfix)
    packComponents(deps.modularMachineryReborn)
    packComponents(deps.modularMachineryRebornMekanism)
    packComponents(deps.moogsVoyagerStructures)
    packComponents(deps.moreBrushes)
    packComponents(deps.moreConcrete)
    packComponents(deps.moreDelight)
    packComponents(deps.mouseTweaks)
    packComponents(deps.myNethersDelight)
    packComponents(deps.mysticalOakTree)
    packComponents(deps.mythsAndLegends)
    packComponents(deps.namelessTrinkets)
    packComponents(deps.naturesCompass)
    packComponents(deps.netherChested)
    packComponents(deps.netherDepthsUpgrade)
    packComponents(deps.nightvisionSpyglass)
    packComponents(deps.nightworld)
    packComponents(deps.noisium)
    packComponents(deps.notenoughcrashes)
    packComponents(deps.notenoughrecipebook)
    packComponents(deps.nullscape)
    packComponents(deps.oceansDelight)
    packComponents(deps.ohTheBiomesWeveGone)
    packComponents(deps.omnitools)
    packComponents(deps.optitritium)
    packComponents(deps.oritech)
    packComponents(deps.oritechThings)
    packComponents(deps.overflowingBars)
    packComponents(deps.packagedauto)
    packComponents(deps.packagedavaritia)
    packComponents(deps.packagedmekemicals)
    packComponents(deps.parcool)
    packComponents(deps.particleEffects)
    packComponents(deps.particleRain)
    packComponents(deps.pastelMod)
    packComponents(deps.perSpatium)
    packComponents(deps.pickUpNotifier)
    packComponents(deps.piglinProliferation)
    packComponents(deps.pillagerCaravans)
    packComponents(deps.pingWheel)
    packComponents(deps.plonk)
    packComponents(deps.pocketDimension1201)
    packComponents(deps.polymorph)
    packComponents(deps.powah)
    packComponents(deps.qliphothAwakening)
    packComponents(deps.quark)
    packComponents(deps.railwaysUntold)
    packComponents(deps.rangedWirelessRedstone)
    packComponents(deps.reAvaritia)
    packComponents(deps.reconnectibleChains)
    packComponents(deps.refinedStorage)
    packComponents(deps.refinedStorageCuriosIntegration)
    packComponents(deps.refinedStorageEmiIntegration)
    packComponents(deps.refinedStorageMekanismIntegration)
    packComponents(deps.refinedStorageQuartzArsenal)
    packComponents(deps.responsiveshields)
    packComponents(deps.ribbits)
    packComponents(deps.rollDelight)
    packComponents(deps.rollingDownInTheDeep)
    packComponents(deps.rottencreatures)
    packComponents(deps.rsinfinitybooster)
    packComponents(deps.rusticEngineer)
    packComponents(deps.sereneSeasons)
    packComponents(deps.shortCircuit)
    packComponents(deps.simpleConveyorBelts)
    packComponents(deps.simpleRecallPotion)
    packComponents(deps.simplymore)
    packComponents(deps.simplySwords)
    packComponents(deps.slagNEmbers)
    packComponents(deps.slavicDelight)
    packComponents(deps.smoothSwapping)
    packComponents(deps.sneakThroughBerries)
    packComponents(deps.sodium)
    packComponents(deps.solarFluxReborn)
    packComponents(deps.sophisticatedBackpacks)
    packComponents(deps.spark)
    packComponents(deps.sparkles)
    packComponents(deps.sparsestructures)
    packComponents(deps.spectralEnchantment)
    packComponents(deps.spyglassImprovements)
    packComponents(deps.statusEffectBarsReforged)
    packComponents(deps.stellaris)
    packComponents(deps.stellarisStellarviewCompatibility)
    packComponents(deps.stylishEffects)
    packComponents(deps.sunkenSpires)
    packComponents(deps.swiftnessEnchantment)
    packComponents(deps.terraCurio)
    packComponents(deps.terralith)
    packComponents(deps.theTwilightForest)
    packComponents(deps.timeInABottleUniversal)
    packComponents(deps.tinyItemAnimations)
    packComponents(deps.tinySkeletons)
    packComponents(deps.tips)
    packComponents(deps.tooFast)
    packComponents(deps.tooltipOverhaul)
    packComponents(deps.trailtalesDelight)
    packComponents(deps.trashCans)
    packComponents(deps.trashslot)
    packComponents(deps.travelersTitles)
    packComponents(deps.treeGiant)
    packComponents(deps.treeVeinMiner)
    packComponents(deps.trek)
    packComponents(deps.tslatentitystatus)
    packComponents(deps.tweakerge)
    packComponents(deps.twilightDelight)
    packComponents(deps.twilightForestFinalBossRemake)
    packComponents(deps.twilightTreehouses)
    packComponents(deps.typewriterDaycounter)
    packComponents(deps.universalGrid)
    packComponents(deps.universalSawmill)
    packComponents(deps.upgradedMobs)
    packComponents(deps.urkazMoonTools)
    packComponents(deps.usefulRailroads)
    packComponents(deps.usefulmagic)
    packComponents(deps.vanillabackport)
    packComponents(deps.villagesAndPillages)
    packComponents(deps.visionEnchantment)
    packComponents(deps.visualWorkbench)
    packComponents(deps.waystones)
    packComponents(deps.wfsCaveOverhaul)
    packComponents(deps.whatAreTheyUpTo)
    packComponents(deps.whatsThatSlot)
    packComponents(deps.whenDungeonsArise)
    packComponents(deps.whenDungeonsAriseSevenSeas)
    packComponents(deps.wingsOfFire)
    packComponents(deps.witheringHeights)
    packComponents(deps.witherSpawnAnimation)
    packComponents(deps.wits)
    packComponents(deps.worldedit)
    packComponents(deps.xaerosMinimap)
    packComponents(deps.xaeroTrainMap)
    packComponents(deps.yacl)
    packComponents(deps.yggdrasilStructure)
    packComponents(deps.yukamisSophisticatedBackpackTab)
    packComponents(deps.yungsBetterDesertTemples)
    packComponents(deps.yungsBetterDungeons)
    packComponents(deps.yungsBetterEndIsland)
    packComponents(deps.yungsBetterJungleTemples)
    packComponents(deps.yungsBetterMineshafts)
    packComponents(deps.yungsBetterNetherFortresses)
    packComponents(deps.yungsBetterOceanMonuments)
    packComponents(deps.yungsBetterStrongholds)
    packComponents(deps.yungsBetterWitchHuts)
    packComponents(deps.yungsBridges)
    packComponents(deps.yungsExtras)
    packComponents(deps.ziplinesRezipped)
    packComponents(deps.zombieAwareness)

    packMods(project(":mods:berlords-carving"))
    packMods(project(":mods:berlords-food-system"))
    packMods(project(":mods:bertie-blackhole"))
    packMods(project(":mods:bertie-emi"))
    packMods(project(":mods:bertie-filters"))
    packMods(project(":mods:bertie-progression"))
    packMods(project(":mods:bertie-tiers"))
    packMods(project(":mods:bertie-toolcraft"))
    packMods(project(":mods:bertie-weapons"))
    packMods(project(":mods:bush-tweaks"))
    packMods(project(":mods:cataclysm-fortresses"))
    packMods(project(":mods:config-migrations"))
    packMods(project(":mods:coo-particles-fix"))
    packMods(project(":mods:cultural-delights-fix"))
    packMods(project(":mods:ender-eyes"))
    packMods(project(":mods:explode-to-mine"))
    packMods(project(":mods:fart-bomb"))
    packMods(project(":mods:forge-ink"))
    packMods(project(":mods:frozen-reg-fix"))
    packMods(project(":mods:hephaestus-architecture"))
    packMods(project(":mods:primitive-refined"))
    packMods(project(":mods:rustic-engineer-fix"))
    packMods(project(":mods:explosive-enhancement"))
    packMods(project(":mods:fd-shader-fix"))
    packMods(project(":mods:screenshot-copy"))
    packMods(project(":mods:short-circuit-fix"))
    packMods(project(":mods:withered-hearts"))
    packMods(project(":mods:withering-waver"))
}
