package com.teamresourceful.resourcefulconfig.api.types.options;

/** Minimal Resourceful Config entry kinds used by the integration fixtures. */
public enum EntryType {
    BYTE,
    SHORT,
    INTEGER,
    LONG,
    FLOAT,
    DOUBLE,
    BOOLEAN,
    STRING,
    ENUM,
    OBJECT,
    CANNOT_BE_PARSED
}
