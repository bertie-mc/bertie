name: Export Modrinth pack
description: Generate a Modrinth client pack directly from its release profile

inputs:
  component:
    description: Pack component subject
    required: false
    default: pack
  output:
    description: Output .mrpack path
    required: true

runs:
  using: composite
  steps:
    - name: Export Modrinth client pack
      shell: bash
      env:
        BERTIE_COMPONENT: ${{ inputs.component }}
        BERTIE_OUTPUT: ${{ inputs.output }}
      run: >-
        bertie-ci pack-export-client --workspace . --component "$BERTIE_COMPONENT"
        --output "$BERTIE_OUTPUT"
